############################################################
# Asterisk ARI Connection
############################################################

# ARI base URL (HTTP, not WS)
ARI_URL=http://127.0.0.1:8088

# ARI user defined in /etc/asterisk/ari.conf
ARI_USER=broadconvo
ARI_PASS=StrongPasswordHere

# ARI Stasis application name (must match dialplan)
ARI_APP=bc-ari-snoop


############################################################
# ExternalMedia → ASR RTP Settings
############################################################

# IP address of the ASR Gateway server
ASR_RTP_HOST=10.10.10.50

# Base UDP port for RTP streams (even number)
# Each call consumes 2 ports:
#   IN  = base + N
#   OUT = base + N + 2
ASR_RTP_BASE_PORT=30000

# Audio codec sent by Asterisk ExternalMedia
# MUST be slin (PCM 16-bit signed, 8kHz)
ASR_RTP_CODEC=slin

# Use deterministic ports derived from LINKEDID
# true  = predictable port allocation
# false = random allocation
ASR_RTP_DETERMINISTIC_PORTS=true


############################################################
# ASR Gateway Registration Endpoint
############################################################

# HTTP endpoint exposed by Qwen ASR Gateway
ASR_GATEWAY_HTTP=http://10.10.10.50:7070


############################################################
# Logging
############################################################

# Supported: error | warn | info | debug
LOG_LEVEL=info
